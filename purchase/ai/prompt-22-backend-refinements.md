use utils.getExecutionContext().canShowAlert, utils.getExecutionContext().canShowToast before toast( ) is called within this application 

use utils.getExecutionContext().canShowAlert, utils.getExecutionContext().canShowToast before alert( ) is called within this application 
